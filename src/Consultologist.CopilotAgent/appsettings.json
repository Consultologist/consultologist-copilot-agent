{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": "Placeholders in {{...}} are filled per environment. Real client ids, tenant id and secrets go in appsettings.Development.json (git-ignored) locally, or App Service configuration in Azure — never here.",

  "Engine": {
    "//": "The Consultologist engine the agent calls as the signed-in clinician.",
    "ApiHost": "https://east.ca.api.consultologist.ai/api",
    "DocumentInputSlot": "referral",
    "PollIntervalSeconds": 3
  },

  "TokenValidation": {
    "//": "Audiences is the Azure Bot registration's client id; validates inbound Bot Service / Entra tokens.",
    "Audiences": [ "{{AgentClientId}}" ],
    "TenantId": "{{TenantId}}"
  },

  "AgentApplication": {
    "StartTypingTimer": true,
    "UserAuthorization": {
      "AutoSignIn": true,
      "DefaultHandlerName": "api",
      "Handlers": {
        "api": {
          "Settings": {
            "//": "AzureBotOAuthConnectionName is the OAuth connection on the Azure Bot (Teams SSO). OBOConnectionName names an entry in Connections. OBOScopes set here means GetTurnTokenAsync returns the exchanged downstream token.",
            "AzureBotOAuthConnectionName": "teams_sso",
            "OBOConnectionName": "ServiceConnection",
            "OBOScopes": [ "api://b3866040-8bae-4c01-88ba-ecff646df451/access_as_user" ]
          }
        }
      }
    }
  },

  "Connections": {
    "ServiceConnection": {
      "Settings": {
        "//": "Prefer FederatedCredentials + a User-Assigned Managed Identity in Azure (Teams SSO requires federated credentials). ClientSecret is shown for local dev only.",
        "AuthType": "ClientSecret",
        "AuthorityEndpoint": "https://login.microsoftonline.com/{{TenantId}}",
        "ClientId": "{{AgentClientId}}",
        "ClientSecret": "{{AgentClientSecret}}",
        "Scopes": [ "https://api.botframework.com/.default" ]
      }
    }
  },
  "ConnectionsMap": [
    {
      "ServiceUrl": "*",
      "Connection": "ServiceConnection"
    }
  ]
}
